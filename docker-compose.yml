services:
  app:
    image: cdcd/php-cicd-demo:${APP_VERSION:-latest}
    container_name: php-cicd-demo
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - APP_VERSION=${APP_VERSION:-latest}
      - BUILD_DATE=${BUILD_DATE:-unknown}
